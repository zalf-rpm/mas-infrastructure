@* <Router AppAssembly="@typeof(App).Assembly" AdditionalAssemblies="new[] { typeof(SharedDemo.Home).Assembly }"> *@
<Router AppAssembly="@typeof(App).Assembly">
    <Found Context="routeData">
        @* <RouteView RouteData="@routeData" DefaultLayout="@typeof(SharedDemo.Layouts.DocsLayout)" /> *@
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
        @* <FocusOnNavigate RouteData="@routeData" Selector="h1" /> *@
    </Found>
    <NotFound>
        @* <LayoutView Layout="@typeof(SharedDemo.Layouts.EmptyLayout)"> *@
        @*     <p>Sorry, there's nothing at this address.</p> *@
        @* </LayoutView> *@
        <PageTitle>Not found</PageTitle>
        <LayoutView Layout="@typeof(MainLayout)">
            <p role="alert">Sorry, there's nothing at this address.</p>
        </LayoutView>
    </NotFound>
</Router>
